# Getting Started

TSXMOD is an easy way to make large-scale, non-trivial changes to codebases. Refactor code, migrate to new APIs, and better understand your codebase.

## Installation

Install the package via composer:

```bash
npm install tsxmod
```
