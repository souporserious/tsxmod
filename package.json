{
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "build:packages": "turbo run build --filter=tsxmod --filter=utils",
    "ci:version": "pnpm changeset version && pnpm install --no-frozen-lockfile && git add .",
    "ci:publish": "pnpm build:packages && pnpm publish -r --access public --publish-branch main"
  },
  "devDependencies": {
    "@changesets/cli": "^2.26.1",
    "@swc/core": "^1.3.61",
    "@swc/jest": "^0.2.26",
    "@types/jest": "^29.5.2",
    "@types/node": "20.2.5",
    "@types/react": "18.2.8",
    "@types/react-dom": "18.2.4",
    "jest": "^29.5.0",
    "tsup": "^6.7.0",
    "turbo": "latest",
    "typescript": "4.9.5"
  }
}
